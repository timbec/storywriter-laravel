*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db
repomix*

# Terraform - Infrastructure as Code
# Exclude local state and sensitive files, but keep .tf code files

# Local Terraform directories (plugins, modules cache)
**/.terraform/
**/.terraform.lock.hcl

# State files (should be in S3, not in git)
*.tfstate
*.tfstate.*
*.tfstate.backup

# Plan files
*.tfplan
tfplan

# Variable files with sensitive values (DO NOT COMMIT)
terraform.tfvars
*.auto.tfvars

# Keep example variable files
!*.tfvars.example

# Crash log files
crash.log
crash.*.log

# Override files (local customizations)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI configuration files
.terraformrc
terraform.rc